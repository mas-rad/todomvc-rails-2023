<header class="header">
  <h1>todos</h1>
  <%= form_with(model: Todo.new, id: 'new-todo') do |form| %>
    <%= form.text_field :title, class: "new-todo", placeholder: "What needs to be done?", autofocus: true %>
  <% end %>
</header>
